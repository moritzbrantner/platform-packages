---
"@moritzbrantner/flat-design": minor
---

Add a direct-manipulation flat-design workbench with multi-selection, marquee selection, drag/resize/rotate handles, snapping guides, keyboard nudging, undo/redo, primitive creation, layer management, grouping, and shared immutable editor commands. Use the workbench in the SVG interchange editor while preserving the existing FlatSceneEditor API.
