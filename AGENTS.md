# Repository Instructions

- After each completed implementation step that changes the workspace, run `bun install --frozen-lockfile` from `/home/moenarch/moritzbrantner/platform-packages`.
- Treat lockfile stability as mandatory: do not update dependencies, but updating `bun.lock` is allowed when needed to keep the workspace consistent.
- If `bun install --frozen-lockfile` fails, stop and report the failure before continuing with more changes.
