# @moritzbrantner/oxfmt-config

Shared Oxfmt defaults for workspace packages and scaffold consumers.

## Main API

- `./oxfmt.json`

## Notes

- The exported config mirrors the formatter settings used at the repository root.
