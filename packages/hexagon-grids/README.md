# @moritzbrantner/hexagon-grids

H3-based geospatial helpers for hex cells, polygon coverage, and point aggregation.

## Main APIs

- `pointToHexCell(point, resolution)` / `getHexCell(cellId)`
- `getHexCellsForPolygon(input, resolution, options)`
- `aggregatePointsToHexGrid(points, options)`
