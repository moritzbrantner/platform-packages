# @moritzbrantner/collaboration

Automerge-backed collaboration store utilities plus React overview components.

## Main APIs

- `createCollaborationStore(options)` / `loadCollaborationStore(options)`
- `createCollaborationOverview(store, options)`
- `CollaborationOverviewView`, `CollaborationOverviewTable`, and `CollaborationOverviewTree`
