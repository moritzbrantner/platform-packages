# @moritzbrantner/tree-structures

Generic tree indexing, flattening, and traversal helpers for parent-linked node sets.

## Main APIs

- `createTreeIndex(nodes, options)`
- `flattenTree(roots, options)`
- `getTreeStats(roots, options)`
